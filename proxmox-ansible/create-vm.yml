---
- name: Neue VM aus Template erstellen
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    proxmox_host: "192.168.30.99"
    proxmox_user: "root@pam"
    proxmox_password: "********"
    node: "testpve"
    template_name: "debian13-auto-11"

  tasks:

    - name: VM klonen
      community.proxmox.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ node }}"
        clone: "{{ template_name }}"
        name: "{{ vm_name }}"
        newid: "{{ vmid }}"
        full: true
        state: present

    - name: Cloud-Init konfigurieren
      community.proxmox.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ node }}"
        vmid: "{{ vmid }}"
        ipconfig:
          ipconfig0: "ip={{ vm_ip }}/24,gw=192.168.30.250"
        ciuser: "admin"
        cipassword: "********"
        sshkeys: "{{ lookup('file', '/root/.ssh/id_ed25519.pub') }}"
        update: true
        state: present

    - name: VM starten
      community.proxmox.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ node }}"
        vmid: "{{ vmid }}"
        state: started
